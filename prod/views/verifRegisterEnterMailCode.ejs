<!DOCTYPE html>
<html lang="fr">
<head>
    <link rel="stylesheet" href="css/style2.css">
    <link rel="stylesheet" href="css/style4.css">
    <title>Vérification : Vérification code</title>
</head>

<body>
    <div class="wrapper">
        <%- include('partials/header') %>
        <main>
            <div class="container">
                <div class="form-box_register">
                    <div class="code-container" id="code">
                        <form action="/verifCode_registerAccount" method="POST">
                            <div class="top">
                                <header>Vérification</header>
                                <span>
                                    Entrez ci-dessous le code de vérification de votre boîte mail <b>sans espaces</b>.<br>
                                    Si vous n'avez rien reçu, vérifiez vos spams ou modifiez votre mail d'inscription <a href="/register">ici</a> (il comporte peut-être une erreur de frappe).
                                </span>
                                <div class="error_msg" id="error_msg">
                                    <% if (error_msg != ""){ %>
                                        <%= error_msg %>
                                    <% } %>
                                </div>
                            </div>
                            <div class="input-box">
                                <input type="text" name="Code" class="input-field" placeholder="Code" required>
                                <i class="bx bx-user"></i>
                            </div>
                            <div class="input-box">
                                <input type="submit" class="submit" value="Valider">
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>